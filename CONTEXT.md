# SiteFade

SiteFade 是一个浏览器扩展项目：对清单内站点（目标站点规则），在访问被记入历史后立即将其访问记录删除，使这些站点不出现在历史中。本上下文覆盖扩展的领域模型与规则语义。

## Language

**目标站点规则（rule）**:
规则清单中的一条条目，决定"哪些 URL 的访问记录要删除"。
_Avoid_: 黑名单（语义上是"受保护站点"而非拦截名单）

**访问记录（visit)**:
浏览器历史中某条 URL 的记录。
_Avoid_: 历史条目（visit 与 URL 的关系是一对多，一条 URL 可有多次访问）

**删除（delete）**:
通过浏览器 history API 移除一条已写入的访问记录。这是本项目的能力边界。
_Avoid_: 阻止（浏览器无"阻止写入历史"的 API，"不进历史"是产品效果描述，不是 API 能力）

**规则清单（rule list）**:
全部目标站点规则的集合，是导入、存储、匹配的对象。

**匹配（match）**:
判断一次访问的 URL 是否命中某条规则。

## 通配符语义

通配符仅用于主机名前缀，一次只能写一个前缀字符（`*`、`+`、`.`），后接域名。

**单级通配符 `*`**:
只匹配一级。`*.baidu.com` 匹配 `tieba.baidu.com`，不匹配 `123.tieba.baidu.com` 或 `baidu.com`。裸 `*` 只匹配不含点的主机名（如 `localhost`）。

**多级含根 `+`**:
匹配根域名及全部子域。`+.baidu.com` 匹配 `baidu.com`、`tieba.baidu.com`、`123.tieba.baidu.com`。

**多级不含根 `.`**:
匹配全部子域但不包含根域名本身。`.baidu.com` 匹配 `tieba.baidu.com`，不匹配 `baidu.com`。

**裸域名（bare domain）**:
无前缀符号的域名（如 `baidu.com`）。默认语义为全站：等价于多级含根 `+`。

## 匹配语义

**精确 URL 规则（exact URL rule）**:
按“scheme + 主机 + 路径”匹配的规则；查询串（query）与片段（#hash）不参与匹配。删除目标始终是访问时的真实 URL，而非规则文本。
_Avoid_: 把精确 URL 理解为整串相等（会放过携带追踪参数的访问）

**IP 字面量（IP literal）**:
IP 地址作规则时的语义为主机精确命中（该主机的全部 URL）；IPv4 与 IPv6（方括号形式）分开处理，不参与域名子域匹配。

**带端口条目（port-qualified entry）**:
形如 `example.com:8080` 的规则，仅精确命中该主机+端口；不带端口的主机/通配符规则匹配时忽略端口（全端口生效）。
_Avoid_: 把端口当作主机名的一部分

## 导入

**规则形态（rule shape）**:
合法规则 = 裸域名 / IP 字面量 / 带端口条目 / 通配符（`*` `+` `.`）/ file:// 精确 URL / http(s) 精确 URL。其余一律无效。
_Avoid_: chrome://、brave://、edge://、about:、data:（无效）

**远程源（remote source）**:
通过 URL 获取规则清单的来源（如 GitHub raw / 自建列表）。拉取方式与刷新策略见地图决策。

**文件/粘贴导入（file/paste import）**:
本地导入渠道：选择文件或粘贴文本。

**规范化（normalize）**:
导入时对每条规则做的标准化：去协议与尾斜杠、统一小写、IDN 转 punycode、去空白；规范化后去重，无效规则报告并跳过。

**来源（source）**:
一条规则来自哪里：手动添加（manual）或某远程源（remote）。手动规则随账号同步；远程内容仅存本机，由各设备自行拉取。

**设置锁（settings lock）**:
设置页的可选 PIN 保护（固定 4 位数字，默认关闭，仅本地哈希存储）。只锁设置页，popup 不锁。用于防同机他人顺手查看，非强加密。
_Avoid_: 密码、强加密（产品定位为简单保护）

## UI 偏好

**主题偏好（theme preference）**:
用户设定的主题模式，取值 `跟随系统 / 浅色 / 深色`。与「系统外观」（`prefers-color-scheme`）区分：前者是设定值，后者是「跟随系统」时的信号源。随账号同步。
_Avoid_: 主题、换肤（过泛，与外观模式混淆）

**界面语言偏好（UI language preference）**:
设置里存储的展示语言选择：`zh_CN` / `en`，或未设定（跟随浏览器语言）。与「浏览器语言（locale）」区分：后者是检测来源，前者是设定值，两者可以不一致（切换偏好 ≠ 修改浏览器语言）。随账号同步。
_Avoid_: 语言、locale（与浏览器语言混淆）

**界面语言（UI language）**:
界面实际使用的展示语言，恒为 `zh_CN` / `en`。设定了偏好即等于偏好；未设定时按浏览器语言解析（`zh`→中文、`en`→英文、其余回退中文基线）。词条缺失时回退中文基线（英文界面可能出现个别中文词条）。切换偏好时同步更新「文档语言」。
_Avoid_: 显示语言、翻译语言（与偏好混淆）

**文档语言（document language）**:
界面在 `<html lang>` 属性上声明的语言，取值 `zh-CN` / `en`，由生效「界面语言」（`zh_CN`/`en`）映射得到。与界面语言区分：后者是内部枚举值，前者是写给浏览器的 BCP-47 标签。
_Avoid_: 界面语言的别名（zh-CN 是标签写法，不是界面语言的取值）

**UI 偏好引导（UI preference bootstrap）**:
入口挂载前读取设置并按序应用主题偏好、界面语言与文档语言的过程，保证首帧即呈现正确外观与语言（避免浅色/默认语言闪烁）。
_Avoid_: 启动序列（过泛，隐含通用编程含义）

**错误标识（error key）**:
业务错误路径产出的稳定字符串标识，界面层按当前界面语言查字典翻译；非标识的原始串（如 HTTP 状态、网络错误文本）原样透出。
_Avoid_: 错误码、错误消息（易与直接展示文案混淆）

**操作提示（toast）**:
设置页操作结果的瞬时反馈（成功/失败/限制命中），展示约 3 秒后自动消失；内容是调用方按「错误标识」契约翻译后的文本，仅本机展示、不持久。
_Avoid_: 通知（会与浏览器通知混淆）